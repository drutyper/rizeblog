<%= simple_form_for([@post, @post.comments.build]) do |f| %>

<div class="field">
  <div class="control">
    <%= f.input :body, input_html: { class: 'textarea' }, wrapper: false, label: 'Comment' %>
  </div>
</div>
<%= f.button :submit, 'Leave a comment', class: "button is-primary" %>
<% end %>